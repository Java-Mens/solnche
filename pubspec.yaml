name: solntsemer
description: "СОЛНЦЕМЕР — отображение истинного местного солнечного времени по координатам наблюдателя."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.3

dependencies:
  flutter:
    sdk: flutter
  astronomia: ^1.1.1
  flutter_riverpod: ^3.4.3
  shared_preferences: ^2.5.5
  intl: ^0.20.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
