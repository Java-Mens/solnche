# Астрономические алгоритмы

Реализация соответствует ТЗ §12 и использует пакет `astronomia` (MIT, чистый Dart,
порт реализации J. Meeus «Astronomical Algorithms», 2-е изд.).

## Истинное местное солнечное время (LAT)

Формула (ТЗ §12.1):

```
LAT = (GAST + λ/15 − α_sun_apparent + 12h) mod 24h
```

где:
- `GAST` — Greenwich Apparent Sidereal Time (часы), вычисляется по `sidereal.apparent(jdUt1)`
- `λ` — долгота точки (градусы, восточная положительная)
- `α_sun_apparent` — видимое прямое восхождение Солнца (часы), вычисляется по `solar.apparentEquatorial(jde)`

Поправка `DUT1 = UT1 − UTC` применяется к аргументу `jdUt1 = jdUtc + DUT1/86400`
(только для GAST; для α_sun и нутации сдвиг пренебрежимо мал: ошибка ≤0.003 с при DUT1 ≤ 0.9 с).

## Уравнение времени

`E = eqtime.e(jde, earth)` — высокоточная формула (§28 Мёус, VSOP87 Earth):

```
E = L0(τ) − 0.0057183° − α + Δψ·cos(ε)
```

где `L0(τ)` — средняя долгота Солнца, `α` — видимое прямое восхождение,
`Δψ` — нутация в долготе, `ε` — истинный наклон эклиптики.

## Восход/заход/полдень

Реализация собственная (аналог astronomia.sunrise, но с настраиваемой высотой h₀):

1. Шагаем по JD-суткам с шагом 5 минут
2. Находим переходы высоты через h₀ (бисекция до ~1 мс)
3. Уточняем кульминацию тернарным поиском

Два режима (FR-18):
- **Наблюдаемый**: h₀ = −0.833° (верхний край диска, стандартная рефракция)
- **Геометрический**: h₀ = 0° (центр диска на геометрическом горизонте)

## ΔT = TT − UT1

- Для 1620–2010: табличная интерполяция (Meeus Table 10.A)
- Для 2000+: полином (Meeus §10)

## DUT1

Интерфейс `Dut1Source` с двумя реализациями:
- `NoDut1Source` — UTC ≈ UT1 (пониженная точность ±0.9 с)
- `TableDut1Source` — встроенная таблица с линейной интерполяцией

## Точность

- Целевая точность LAT: ±1 с при корректных системных часах и DUT1
- Без DUT1: ±1 с + |UT1 − UTC| ≈ ±1.9 с
- Восход/заход: ±1 мин (ограничено рефракцией, рельефом, метеорологией)
